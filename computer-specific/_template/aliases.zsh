# Host-specific aliases for the external host tier.
#
# aliases.zsh is an AGGREGATE file: shell_setup.sh concatenates
# default + each profile (in order) + this host, so universal aliases
# (default tier) and tool aliases (e.g. dev-core git, claude-code-aliases
# cr) are already sourced ahead of this file. Only genuinely
# host-specific entries belong here.

# Add aliases specific to this machine here.

# Example:
# alias ll='ls -la'

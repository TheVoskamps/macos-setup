# 04-Uninstall.versionmanagers: Packages to uninstall — Version managers and environment tooling (asdf, direnv, etc.)
#
# Entries listed here are removed by `make uninstall` /
# `make 04_Uninstall_versionmanagers` and are filtered out of the matching
# `Install/04-Install.versionmanagers` (and same-tier or lower-tier Install files) by the smart
# filter on `make install` — see docs/INSTALL.md.
#
# Format mirrors a Brewfile: `brew '...'`, `cask '...'`, or
# `mas '...', id: NNNNN`. `tap` directives are ignored. Comments and blank
# lines are ignored.

# Taps

# Homebrew formulae

# Casks

# Other formulae

# Mac App Store (mas)

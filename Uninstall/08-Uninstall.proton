# 08-Uninstall.proton: Packages to uninstall — Proton and related software
#
# Entries listed here are removed by `make uninstall` /
# `make 08_Uninstall_proton` and are filtered out of the matching
# `Install/08-Install.proton` (and same-tier or lower-tier Install files) by the smart
# filter on `make install` — see docs/INSTALL.md.
#
# Format mirrors a Brewfile: `brew '...'`, `cask '...'`, or
# `mas '...', id: NNNNN`. `tap` directives are ignored. Comments and blank
# lines are ignored.

# Taps

# Homebrew formulae

# Casks

# Other formulae

# Mac App Store (mas)

# 05-Install.tools: General developer tools and utilities
# Lean default: git is here because bootstrap.sh needs it. gh moved to the
# `dev-core` profile (basic dev tooling, not universal).

# Taps

# Homebrew formulae
brew 'git'
brew 'git-filter-repo'
brew 'wget'
brew 'curl'
brew 'jq'
brew 'yq'
brew 'htop'
brew 'watch'
brew 'openssl@3'
brew 'gnupg'

# Casks
# Other formulae
# Mac App Store (mas)
